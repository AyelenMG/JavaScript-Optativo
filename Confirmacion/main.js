function confirmar()
{
    /*La función confirm hace aparecer una pantalla de confirmación con el mensaje escrito en el parentesis*/
    return confirm("¿Está seguro de querer salir e ir a Pinterest?")
}